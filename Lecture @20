_______________________________________________________________________Policy Based Data Structure________________________________________________________________________________________
Ekhane jei problem ta niye vai boltese ta holo dui type er query dewa thakbe . 
1) ! x  -------> Ei type er query er jonno amader ke element ta ke ekta container e insert korte hobe .
2) ? x  -------> Ei type er query er jonno sorted obosthay container e er position koto ba er theke chuto koyta element ache ta bolte hobe .

Ei problem ta solve korar jonne etodin porjonto ja ja sikhechi ta diye solve kora jacche nah . Karon map ba set diye insert and sorted rakhar kaaj ta kora jabe,
kintu er position koto eita bola jabe na . Oi set er vitore amader ke indexing er subidha prodan korar jonno jei Data Struture ta toiri kora hoyeche ta holo
ordered_set . Eti red-black-tree data structure use kore internally implement kora hoyeche . Eti use korte hole kichu extra header file include korte hoy .
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#include <bits/stdc++.h>
// Next two of the header files are introduced to our template only for using ordered set .
#include <ext/pb_ds/assoc_container.hpp> 
#include <ext/pb_ds/tree_policy.hpp>

using namespace std ;
using namespace __gnu_pbds; // This is the namespace standard for policy based data structure .

template<typename T>
using ordered_set = tree<T, null_type, less<T>, rb_tree_tag, tree_order_statistics_node_update>;
// order_of_key(k) : no. of elements < k . // Er maddhome ordered_set e k er index koto ta pawa jay .
// less_equal<T> for multiset. // Jodi insert kora element gular shobgula occurance niye deal korte hoy tahole amra less<T> er jaygay less_equal<T> use kori .
// *find_by_order(i) : value at index i (0-based) // Er maddhome ordered_set i tomo index e kun value ta ache ta pawa jay .

1) osat.order_of_key(value) will give the index of the value     ,   2) *(osat.find_by_order(index)) will give the value of that index .
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
***) ordered_set ke traverse korar jonne sadharonoto duita method follow kora hoy . 
1) for( auto it = osat.begin() ; it != osat.end() ; it++ ) cout << *it << gap ;     2 ) for( auto val : osat ) cout << val << gap ;

Tobe osat ke normal loop chaliye o iterate kora jay . Tobe ekhetre opeartion gula onekta costly hoye jay . Proti ta value print korte O(log2(osat.size())) complexity hoy .
for(int i = 0 ; i < osat.size() ; i++) cout << *osat.find_by_order(i) << gap ; // Not recommanded . Use only when necessary .

Jodi find_by_order use kora hoy ar out of bound index function er vitore parameter hishabe dewa hoy tahole garbage value return kore . 

___________________________________________________________________________Count of Smaller Numbers After Self____________________________________________________________________________

Question er moddhe amader ke nums naam er ekta array dewa ache . Array er proti ta index er jonne er right e er theke chuto thik koyta element ache oita 
amader ke bolte hobe . Ei question ta solve korar shomoy template e less_equal<T> korte hobe . Karon ekhane value er repeatation ghot te pare .

#include <bits/stdc++.h>
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>
using namespace std ;
using namespace __gnu_pbds;

template<typename T>
using ordered_set = tree<T, null_type, less_equal<T>, rb_tree_tag, tree_order_statistics_node_update>;
class Solution {
public:
    vector<int> countSmaller(vector<int>& nums) {
        ordered_set<int> osat ; vector<int> ans ;
        for(int i = nums.size()-1 ; i >= 0 ; i--){
            ans.push_back(osat.order_of_key(nums[i])) ; osat.insert(nums[i]) ;
        }
        reverse(ans.begin(),ans.end()) ;
        return ans ;
    }
};

This is a hard tag question of LEETCODE . Ei proshno ta jodi ordered_set diye na kortam tahole kora onek kothin hoye jeto . Kintu ordered_set diye korar karone 
khub e sohoje amra question ta solve kore felte parsi .
____________________________________________________________________________________X____________________________________________________________________________________________________
